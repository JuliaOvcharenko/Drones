import { client } from "../client/client";
import { Prisma } from "../generated/prisma";
import { AddressRepositoryContract } from "./address.types";


export const AddressRepository: AddressRepositoryContract = {
    createAddress: async(data)=>{
        try{

        }
        catch(error){

        }
    }
}